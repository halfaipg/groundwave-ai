# groundwave-ai Environment Configuration
# Copy this file to .env and customize for your deployment
# ═══════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────
# COMMUNITY BRANDING
# ─────────────────────────────────────────────────────────────
COMMUNITY_NAME="My Mesh Network"
COMMUNITY_DESCRIPTION="A local community mesh network"
COMMUNITY_TAGLINE="Offline-first community mesh"
LOCATION_NAME="Your City"

# Bot identity on the mesh
BOT_SHORT_NAME="ai01"
BOT_LONG_NAME="MeshBot AI"
BOT_PREFIX="🤖"

# ─────────────────────────────────────────────────────────────
# MESH PROTOCOL & CONNECTION
# ─────────────────────────────────────────────────────────────
# Protocol: meshtastic or meshcore
# MeshCore recommended for new deployments
MESH_PROTOCOL="meshtastic"

# Connection type: serial, tcp, or ble
MESH_CONNECTION_TYPE="serial"

# Serial port (find with: ls /dev/cu.* on macOS, ls /dev/ttyUSB* on Linux)
MESH_SERIAL_PORT="/dev/cu.usbmodem1234"

# TCP connection (if using WiFi)
MESH_TCP_HOST="meshtastic.local"
MESH_TCP_PORT="4403"

# ─────────────────────────────────────────────────────────────
# LOCAL LLM (LM Studio / Ollama)
# ─────────────────────────────────────────────────────────────
LLM_PROVIDER="lmstudio"

# LM Studio settings
LMSTUDIO_URL="http://localhost:1234/v1/chat/completions"
LMSTUDIO_MODEL="local-model"

# Ollama settings (alternative)
OLLAMA_URL="http://localhost:11434/api/generate"
OLLAMA_MODEL="llama3.2"

# ─────────────────────────────────────────────────────────────
# WEB PORTAL
# ─────────────────────────────────────────────────────────────
WEB_HOST="0.0.0.0"
WEB_PORT="8000"

# Admin panel
ADMIN_ENABLED="true"
ADMIN_PASSWORD="changeme"
# Admin access: "localhost" (default, most secure), "local" (LAN), or "all"
ADMIN_ACCESS="localhost"

# ─────────────────────────────────────────────────────────────
# REGIONAL MQTT (Optional - for regional mesh awareness)
# ─────────────────────────────────────────────────────────────
MQTT_ENABLED="false"
MQTT_BROKER="mqtt.example.org"
MQTT_PORT="1883"
MQTT_USERNAME=""
MQTT_PASSWORD=""
MQTT_TOPIC="msh/US/#"
MQTT_REGION_NAME="Regional"

# ─────────────────────────────────────────────────────────────
# WEATHER (Open-Meteo - free, no API key needed)
# ─────────────────────────────────────────────────────────────
WEATHER_ENABLED="true"
# Default location (latitude, longitude)
WEATHER_DEFAULT_LAT="40.7128"
WEATHER_DEFAULT_LON="-74.0060"

# ─────────────────────────────────────────────────────────────
# KIWIX / OFFLINE WIKIPEDIA (Optional)
# ─────────────────────────────────────────────────────────────
KIWIX_ENABLED="false"
KIWIX_URL="http://localhost:8080"
KIWIX_LIBRARY="wikipedia_en_simple"
KIWIX_AI_ENHANCED="true"

# ─────────────────────────────────────────────────────────────
# BRANDING ASSETS (place your own in app/static/)
# ─────────────────────────────────────────────────────────────
# Default groundwave branding included - customize with your own:
FAVICON="/static/groundwave-favicon.svg"
LOGO_LARGE="/static/groundwave-logo.svg"
LOGO_SMALL="/static/groundwave-icon.svg"

# ─────────────────────────────────────────────────────────────
# PATHS (usually don't need to change)
# ─────────────────────────────────────────────────────────────
# Database location
DATABASE_PATH="data/groundwave.db"
